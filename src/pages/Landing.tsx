import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { api } from "@/convex/_generated/api";
import { useQuery } from "convex/react";
import { motion } from "framer-motion";
import { ArrowLeft, ArrowUpLeft, Bus, CalendarDays, CheckCircle2, ChevronLeft, Clock3, MapPin, Route, Search, ShieldCheck, Sparkles, Ticket, Users, Building2, WalletCards } from "lucide-react";
import { useMemo, useState } from "react";
import { Link } from "react-router";
import { toArabicIndic } from "@/lib/arabic";
import { useLocations } from "@/hooks/use-locations";

const steps = [
  { n: "١", title: "ابحث", text: "حدد مدينة الانطلاق والوجهة وتاريخ السفر." },
  { n: "٢", title: "قارن", text: "قارن شركات النقل والرحلات والأسعار والمقاعد." },
  { n: "٣", title: "اختر", text: "راجع تفاصيل الرحلة واختر الخيار الأنسب لك." },
  { n: "٤", title: "احجز", text: "أكمل الحجز واحصل على تذكرتك عبر المنصة." },
];

const destinations = [
  { name: "الرياض", type: "مدينة انطلاق", tone: "from-slate-950 to-slate-700" },
  { name: "جدة", type: "بوابة السفر", tone: "from-sky-950 to-cyan-700" },
  { name: "مكة", type: "منطقة الحجاز", tone: "from-amber-950 to-amber-700" },
  { name: "صنعاء", type: "وجهة رئيسية", tone: "from-emerald-950 to-emerald-700" },
  { name: "عدن", type: "بوابة الجنوب", tone: "from-indigo-950 to-indigo-700" },
  { name: "حضرموت", type: "شرق اليمن", tone: "from-orange-950 to-orange-700" },
];

function tripTime(value?: string) {
  if (!value) return "حسب الجدول";
  const [h, m] = value.split(":").map(Number);
  if (!Number.isFinite(h) || !Number.isFinite(m)) return value;
  const suffix = h >= 12 ? "م" : "ص";
  const hour = h % 12 || 12;
  return `${toArabicIndic(hour)}:${String(m).padStart(2, "0")} ${suffix}`;
}

function duration(departure?: string, arrival?: string) {
  if (!departure || !arrival) return "حسب جدول الرحلة";
  const [dh, dm] = departure.split(":").map(Number);
  const [ah, am] = arrival.split(":").map(Number);
  if (![dh, dm, ah, am].every(Number.isFinite)) return "حسب جدول الرحلة";
  let minutes = ah * 60 + am - (dh * 60 + dm);
  if (minutes < 0) minutes += 24 * 60;
  const hours = Math.floor(minutes / 60);
  const mins = minutes % 60;
  return mins ? `${toArabicIndic(hours)} س ${toArabicIndic(mins)} د` : `${toArabicIndic(hours)} ساعات`;
}

function formatTravelDate(value: string) {
  if (!value) return "اختر تاريخ السفر";
  const [year, month, day] = value.split("-").map(Number);
  if (![year, month, day].every(Number.isFinite)) return "اختر تاريخ السفر";
  return `${toArabicIndic(day)} / ${toArabicIndic(month)} / ${toArabicIndic(year)}`;
}

export default function Landing() {
  const trips = useQuery(api.trips.list, {});
  const activeCompanies = useQuery(api.companies.listActive);
  const { saudiCities, yemenCities } = useLocations();
  const [from, setFrom] = useState("all");
  const [to, setTo] = useState("all");
  const [date, setDate] = useState("");
  const companies = activeCompanies ?? [];
  const companyById = useMemo(() => new Map(companies.map((company) => [company.slug, company])), [companies]);
  const filteredTrips = useMemo(() => (trips ?? []).filter((trip) => (from === "all" || trip.from === from) && (to === "all" || trip.to === to)), [trips, from, to]);
  const query = new URLSearchParams();
  if (from !== "all") query.set("from", from);
  if (to !== "all") query.set("to", to);
  if (date) query.set("date", date);
  const customerPath = query.toString() ? `/customer?${query.toString()}` : "/customer";
  const bookingHref = `/auth?returnTo=${encodeURIComponent(customerPath)}`;
  const reset = () => { setFrom("all"); setTo("all"); setDate(""); };

  return (
    <div dir="rtl" className="min-h-screen overflow-x-hidden bg-background text-foreground">
      <header className="sticky top-0 z-50 border-b bg-background/90 backdrop-blur-xl"><div className="mx-auto flex h-[76px] max-w-7xl items-center justify-between gap-4 px-4 sm:px-6"><Link to="/" className="flex shrink-0 items-center gap-3"><img src="/saturn-lines-logo.svg" alt="خطوط زحل" className="h-11 w-auto object-contain" /><div className="hidden leading-tight sm:block"><p className="font-black text-primary">خطوط زحل</p><p className="text-[10px] text-muted-foreground">منصة حجز ومقارنة النقل البري</p></div></Link><nav className="hidden items-center gap-7 text-sm font-bold text-muted-foreground lg:flex"><a href="#search" className="transition hover:text-primary">ابحث عن رحلة</a><a href="#destinations" className="transition hover:text-primary">الوجهات</a><a href="#companies" className="transition hover:text-primary">شركات النقل</a><a href="#how" className="transition hover:text-primary">كيف تعمل؟</a></nav><Button asChild className="gap-2 rounded-xl px-5 shadow-lg shadow-primary/15"><Link to={bookingHref}>دخول <ArrowLeft className="size-4" /></Link></Button></div></header>
      <main>
        <section className="relative isolate overflow-hidden bg-[#061426] text-white"><div className="absolute inset-0 bg-[radial-gradient(circle_at_15%_20%,rgba(242,196,95,.20),transparent_28%),radial-gradient(circle_at_85%_10%,rgba(26,90,130,.42),transparent_34%),linear-gradient(135deg,#061426,#0a2946)]" /><div className="absolute inset-x-0 bottom-0 h-48 bg-gradient-to-t from-[#061426] to-transparent" /><div className="absolute right-[8%] top-24 size-64 rounded-full border border-white/5" /><div className="absolute right-[13%] top-36 size-44 rounded-full border border-[#f2c45f]/10" /><div className="relative mx-auto grid min-h-[650px] max-w-7xl items-center gap-12 px-4 py-16 sm:px-6 lg:grid-cols-[1.05fr_.95fr] lg:py-24">
          <motion.div initial={{ opacity: 0, x: 28 }} animate={{ opacity: 1, x: 0 }} transition={{ duration: .7 }}><div className="mb-5 inline-flex items-center gap-2 rounded-full border border-white/15 bg-white/10 px-4 py-2 text-xs font-bold backdrop-blur-xl"><Sparkles className="size-3.5 text-[#f2c45f]" /> منصة تجمع المسافر بشركات النقل البري</div><h1 className="max-w-3xl text-4xl font-black leading-[1.08] tracking-tight sm:text-5xl lg:text-[4.35rem]">إلى اليمن…<br /><span className="text-[#f2c45f]">رحلتك تبدأ من اختيارك</span></h1><p className="mt-6 max-w-2xl text-base leading-8 text-white/75 sm:text-lg">قارن رحلات النقل البري من السعودية إلى اليمن، اكتشف خيارات متعددة من شركات النقل المستقلة، ثم اختر الرحلة المناسبة لك واحجز عبر المنصة.</p><div className="mt-8 flex flex-wrap gap-3"><Button asChild size="lg" className="rounded-xl bg-[#f2c45f] px-7 font-black text-[#10243c] hover:bg-[#f7d67c]"><a href="#search">ابحث عن رحلتك <Search className="size-5" /></a></Button><Button asChild size="lg" variant="outline" className="rounded-xl border-white/20 bg-white/5 px-7 text-white hover:bg-white/10 hover:text-white"><a href="#destinations">استكشف الوجهات <ArrowUpLeft className="size-5" /></a></Button></div><div className="mt-8 flex flex-wrap gap-x-6 gap-y-3 text-xs font-semibold text-white/65"><span className="flex items-center gap-2"><ShieldCheck className="size-4 text-[#f2c45f]" /> شركات نقل مستقلة</span><span className="flex items-center gap-2"><CheckCircle2 className="size-4 text-[#f2c45f]" /> مقارنة قبل الحجز</span><span className="flex items-center gap-2"><Ticket className="size-4 text-[#f2c45f]" /> حجز وتذكرة إلكترونية</span></div></motion.div>
          <motion.div initial={{ opacity: 0, y: 25 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: .75, delay: .1 }} className="relative"><div className="absolute -inset-6 rounded-[3rem] bg-primary/10 blur-3xl" /><div className="relative overflow-hidden rounded-[2rem] border border-white/15 bg-white/10 p-3 shadow-2xl backdrop-blur-xl"><div className="overflow-hidden rounded-[1.5rem] bg-white text-[#10243c]"><div className="relative h-64 overflow-hidden bg-gradient-to-br from-[#0c3558] via-[#123e5f] to-[#071827]"><div className="absolute -left-10 -top-12 size-56 rounded-full border-[28px] border-white/5" /><div className="absolute bottom-0 left-0 right-0 h-28 bg-gradient-to-t from-black/50 to-transparent" /><div className="absolute inset-x-6 top-6 flex items-center justify-between"><span className="rounded-full border border-white/15 bg-white/10 px-3 py-1.5 text-[10px] font-black text-white backdrop-blur">رحلة عبر خطوط زحل</span><Bus className="size-8 text-[#f2c45f]" /></div><div className="absolute inset-x-6 bottom-6 text-white"><p className="text-xs text-white/60">مسار نموذجي</p><div className="mt-1 flex items-center gap-3 text-3xl font-black"><span>جدة</span><Route className="size-6 text-[#f2c45f]" /><span>صنعاء</span></div><p className="mt-2 text-xs text-white/60">اختر من الرحلات المتاحة وشركات النقل المنفذة</p></div></div><div className="grid grid-cols-3 gap-3 p-5"><div className="rounded-2xl bg-slate-50 p-4"><Clock3 className="size-5 text-primary" /><p className="mt-3 text-[10px] text-muted-foreground">الموعد</p><p className="mt-1 text-sm font-black">حسب الرحلة</p></div><div className="rounded-2xl bg-slate-50 p-4"><WalletCards className="size-5 text-primary" /><p className="mt-3 text-[10px] text-muted-foreground">السعر</p><p className="mt-1 text-sm font-black">قارن أولًا</p></div><div className="rounded-2xl bg-slate-50 p-4"><Users className="size-5 text-primary" /><p className="mt-3 text-[10px] text-muted-foreground">المقاعد</p><p className="mt-1 text-sm font-black">حسب التوفر</p></div></div><div className="flex items-center justify-between border-t px-5 py-4"><div className="flex items-center gap-2 text-xs font-bold"><Bus className="size-4 text-primary" /> شركة النقل تظهر بوضوح في تفاصيل الرحلة</div><span className="text-[10px] font-bold text-muted-foreground">رحلتك. اختيارك.</span></div></div></div></motion.div>
        </div></section>
        <section id="search" className="relative z-20 mx-auto -mt-8 max-w-7xl px-4 sm:px-6"><motion.div initial={{ opacity: 0, y: 15 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} className="rounded-[1.75rem] border bg-card p-4 shadow-2xl shadow-slate-900/10 sm:p-6"><div className="mb-5 flex items-center justify-between gap-4"><div className="flex items-center gap-3"><div className="flex size-11 items-center justify-center rounded-2xl bg-primary/10 text-primary"><Search className="size-5" /></div><div><h2 className="font-black">ابحث عن رحلتك</h2><p className="text-xs leading-6 text-muted-foreground">اختر المسار والتاريخ ثم انتقل مباشرة إلى نتائج الحجز.</p></div></div><span className="hidden rounded-full bg-muted px-3 py-1.5 text-[11px] font-bold text-muted-foreground sm:block">قارن أولًا، ثم احجز</span></div><div className="grid gap-3 md:grid-cols-2 lg:grid-cols-[1fr_1fr_1fr_auto]"><div><p className="mb-2 text-xs font-bold text-muted-foreground">من السعودية</p><Select value={from} onValueChange={setFrom}><SelectTrigger className="h-12 rounded-xl"><SelectValue placeholder="مدينة المغادرة" /></SelectTrigger><SelectContent><SelectItem value="all">كل المدن</SelectItem>{saudiCities.map((city) => <SelectItem key={city} value={city}>{city}</SelectItem>)}</SelectContent></Select></div><div><p className="mb-2 text-xs font-bold text-muted-foreground">إلى اليمن</p><Select value={to} onValueChange={setTo}><SelectTrigger className="h-12 rounded-xl"><SelectValue placeholder="الوجهة" /></SelectTrigger><SelectContent><SelectItem value="all">كل الوجهات</SelectItem>{yemenCities.map((city) => <SelectItem key={city} value={city}>{city}</SelectItem>)}</SelectContent></Select></div><div><p className="mb-2 text-xs font-bold text-muted-foreground">تاريخ السفر</p><div className="relative"><CalendarDays className="pointer-events-none absolute right-3.5 top-3.5 z-10 size-4 text-muted-foreground" /><input type="date" value={date} min={new Date().toISOString().slice(0, 10)} onChange={(e) => setDate(e.target.value)} className="h-12 w-full rounded-xl border bg-background px-10 text-sm outline-none ring-offset-background transition focus:ring-2 focus:ring-primary" /></div></div><div className="flex items-end"><Button asChild className="h-12 w-full rounded-xl px-6 font-black lg:w-auto"><Link to={bookingHref}>عرض الرحلات <ChevronLeft className="size-4" /></Link></Button></div></div>{(from !== "all" || to !== "all" || date) && <div className="mt-4 flex flex-wrap items-center gap-2 text-xs"><Badge variant="secondary">{from === "all" ? "كل المدن" : from}</Badge><Route className="size-3 text-muted-foreground" /><Badge variant="secondary">{to === "all" ? "كل الوجهات" : to}</Badge>{date && <Badge variant="outline">{formatTravelDate(date)}</Badge>}<button type="button" onClick={reset} className="mr-auto font-bold text-muted-foreground underline underline-offset-4 hover:text-primary">مسح البحث</button></div>}</motion.div></section>
        <section className="mx-auto max-w-7xl px-4 py-20 sm:px-6"><div className="mb-8 flex items-end justify-between gap-4"><div><Badge variant="secondary" className="mb-3 rounded-full px-3 py-1">رحلات من بيانات المنصة</Badge><h2 className="text-3xl font-black tracking-tight">خيارات سفر حقيقية قابلة للمقارنة</h2><p className="mt-2 text-sm leading-7 text-muted-foreground">تظهر هنا الرحلات النشطة المسجلة في النظام وفق المسار الذي اخترته.</p></div><Button asChild variant="ghost" className="hidden gap-2 sm:flex"><Link to={customerPath}>كل الرحلات <ArrowLeft className="size-4" /></Link></Button></div>{filteredTrips.length === 0 ? <div className="rounded-3xl border border-dashed bg-card p-10 text-center"><Bus className="mx-auto size-10 text-muted-foreground/50" /><h3 className="mt-4 text-lg font-black">لا توجد رحلات مطابقة حاليًا</h3><p className="mt-2 text-sm text-muted-foreground">جرّب مسارًا آخر أو اعرض جميع الرحلات.</p><Button onClick={reset} variant="outline" className="mt-5 rounded-xl">عرض جميع الرحلات</Button></div> : <div className="grid gap-5 lg:grid-cols-2">{filteredTrips.slice(0, 6).map((trip) => { const company = companyById.get(trip.companyId); return <motion.article key={trip._id} initial={{ opacity: 0, y: 12 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} className="group rounded-3xl border bg-card p-5 shadow-sm transition hover:-translate-y-1 hover:shadow-xl"><div className="flex items-start justify-between gap-4"><div><div className="flex items-center gap-2 text-xs font-bold text-muted-foreground"><MapPin className="size-4 text-primary" />{trip.from}<Route className="size-3" />{trip.to}</div><h3 className="mt-3 text-xl font-black">{company?.name ?? "شركة نقل مشاركة"}</h3><p className="mt-1 text-xs text-muted-foreground">شركة النقل المنفذة للرحلة</p></div><Badge className="rounded-full">متاحة للمقارنة</Badge></div><div className="my-5 grid grid-cols-3 gap-3"><div className="rounded-2xl bg-muted/60 p-3"><p className="text-[10px] text-muted-foreground">المغادرة</p><p className="mt-1 font-black">{tripTime(trip.departureTime)}</p></div><div className="rounded-2xl bg-muted/60 p-3"><p className="text-[10px] text-muted-foreground">الوصول</p><p className="mt-1 font-black">{tripTime(trip.arrivalTime)}</p></div><div className="rounded-2xl bg-muted/60 p-3"><p className="text-[10px] text-muted-foreground">المدة</p><p className="mt-1 font-black">{duration(trip.departureTime, trip.arrivalTime)}</p></div></div><div className="flex flex-wrap items-center justify-between gap-3 border-t pt-4"><div><p className="text-[10px] text-muted-foreground">السعر المعلن</p><p className="text-xl font-black text-primary">{toArabicIndic(trip.price)} <span className="text-xs font-bold text-muted-foreground">ريال</span></p></div><div className="flex items-center gap-3 text-xs font-bold text-muted-foreground"><span className="flex items-center gap-1.5"><Users className="size-4" />{toArabicIndic(trip.availableSeats)} مقعد متاح</span><Button asChild className="rounded-xl"><Link to={bookingHref}>احجز الآن</Link></Button></div></div></motion.article>; })}</div>}</section>
        <section id="destinations" className="border-y bg-muted/30 py-20"><div className="mx-auto max-w-7xl px-4 sm:px-6"><div className="max-w-2xl"><Badge variant="secondary" className="mb-3 rounded-full px-3 py-1">الوجهات</Badge><h2 className="text-3xl font-black">مدن تبدأ منها الرحلة وتصل إليها</h2><p className="mt-3 leading-7 text-muted-foreground">واجهة الوجهات مبنية حول المدن المستخدمة في منظومة البحث، وليست صورًا زخرفية منفصلة عن الخدمة.</p></div><div className="mt-8 grid grid-cols-2 gap-4 sm:grid-cols-3 lg:grid-cols-6">{destinations.map((item, index) => <motion.a key={item.name} href="#search" initial={{ opacity: 0, y: 10 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: index * .04 }} className={`relative min-h-36 overflow-hidden rounded-3xl bg-gradient-to-br ${item.tone} p-5 text-white shadow-sm transition hover:-translate-y-1 hover:shadow-lg`}><div className="absolute -left-8 -top-8 size-28 rounded-full border border-white/10" /><MapPin className="relative size-5 text-[#f2c45f]" /><p className="relative mt-8 text-lg font-black">{item.name}</p><p className="relative mt-1 text-[10px] text-white/60">{item.type}</p></motion.a>)}</div></div></section>
        <section id="companies" className="mx-auto max-w-7xl px-4 py-20 sm:px-6"><div className="flex items-end justify-between gap-4"><div><Badge variant="secondary" className="mb-3 rounded-full px-3 py-1">شركاء النقل</Badge><h2 className="text-3xl font-black">شركات نقل مستقلة على المنصة</h2><p className="mt-2 text-sm leading-7 text-muted-foreground">خطوط زحل منصة حجز ومقارنة، والرحلات تُنفذ بواسطة شركات النقل الموضحة في تفاصيل الحجز.</p></div><Building2 className="hidden size-10 text-primary/30 sm:block" /></div><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{companies.slice(0, 6).map((company) => <div key={company.slug} className="rounded-3xl border bg-card p-5 transition hover:shadow-lg"><div className="flex items-center gap-4"><div className="flex size-12 shrink-0 items-center justify-center rounded-2xl bg-primary/10 text-primary"><Bus className="size-6" /></div><div className="min-w-0"><h3 className="truncate font-black">{company.name}</h3><p className="mt-1 text-xs text-muted-foreground">{company.base ? `المقر: ${company.base}` : "شركة نقل مشاركة"}</p></div></div>{company.routes && <p className="mt-4 line-clamp-2 text-xs leading-6 text-muted-foreground">{company.routes}</p>}<div className="mt-4 flex items-center gap-2 text-[11px] font-bold text-emerald-700"><CheckCircle2 className="size-4" /> متاحة للمقارنة عبر المنصة</div></div>)}</div></section>
        <section id="how" className="bg-[#071a2c] py-20 text-white"><div className="mx-auto max-w-7xl px-4 sm:px-6"><div className="mx-auto max-w-2xl text-center"><Badge className="border-white/10 bg-white/10 text-white">كيف تعمل المنصة؟</Badge><h2 className="mt-4 text-3xl font-black">من البحث إلى التذكرة في خطوات واضحة</h2><p className="mt-3 leading-7 text-white/60">نحن نوفر لك المقارنة والحجز، بينما تنفذ الرحلة شركة النقل المستقلة الموضحة في الحجز.</p></div><div className="mt-12 grid gap-5 md:grid-cols-4">{steps.map((step, index) => <motion.div key={step.n} initial={{ opacity: 0, y: 12 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: index * .08 }} className="relative rounded-3xl border border-white/10 bg-white/[.04] p-6"><div className="flex size-12 items-center justify-center rounded-2xl bg-[#f2c45f] text-lg font-black text-[#10243c]">{step.n}</div><h3 className="mt-6 text-lg font-black">{step.title}</h3><p className="mt-2 text-sm leading-7 text-white/55">{step.text}</p></motion.div>)}</div></div></section>
        <section className="mx-auto max-w-7xl px-4 py-20 sm:px-6"><div className="relative overflow-hidden rounded-[2rem] bg-gradient-to-br from-primary to-[#0b3558] p-8 text-white shadow-2xl sm:p-12"><div className="absolute -left-20 -top-20 size-72 rounded-full border-[35px] border-white/5" /><div className="relative flex flex-col items-start justify-between gap-8 md:flex-row md:items-center"><div><p className="text-sm font-bold text-white/60">خطوط زحل — منصة حجز النقل البري</p><h2 className="mt-3 max-w-2xl text-3xl font-black leading-tight sm:text-4xl">قارن خياراتك، اختر رحلتك، واترك تنفيذ الرحلة لشركة النقل الموضحة في الحجز.</h2></div><Button asChild size="lg" className="shrink-0 rounded-xl bg-[#f2c45f] px-7 font-black text-[#10243c] hover:bg-[#f7d67c]"><Link to={bookingHref}>ابدأ البحث <ArrowLeft className="size-5" /></Link></Button></div></div></section>
      </main>
      <footer className="border-t bg-card"><div className="mx-auto max-w-7xl px-4 py-10 sm:px-6"><div className="flex flex-col gap-6 md:flex-row md:items-start md:justify-between"><div><div className="flex items-center gap-3"><img src="/saturn-lines-logo.svg" alt="خطوط زحل" className="h-10 w-auto" /><div><p className="font-black">خطوط زحل</p><p className="text-xs text-muted-foreground">منصة حجز ومقارنة النقل البري</p></div></div><p className="mt-4 max-w-md text-xs leading-6 text-muted-foreground">خطوط زحل منصة رقمية تربط المسافرين بشركات النقل البري المستقلة وتتيح المقارنة والحجز. لا تملك المنصة الحافلات ولا تنفذ الرحلات بنفسها.</p></div><div className="text-xs text-muted-foreground"><p className="font-black text-foreground">تنبيه مهم</p><p className="mt-2 max-w-sm leading-6">تم إصدار الحجز عبر منصة خطوط زحل، وتُنفذ الرحلة بواسطة شركة النقل الموضحة في التذكرة.</p></div></div><div className="mt-8 border-t pt-5 text-center text-[11px] text-muted-foreground">© {new Date().getFullYear()} خطوط زحل — جميع الحقوق محفوظة</div></div></footer>
    </div>
  );
}
